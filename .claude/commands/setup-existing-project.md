---
description: Install and verify BuilderX orchestration in the current existing project
---

Read `CLAUDE.md`, `AGENTS.md`, and `ROOT_WORKSPACE_GENERATION_POLICY.md` fully.

Then read `.codex/prompts/bootstrap-orchestrator.md` fully and execute it as the canonical bootstrap contract for this existing project.

Use only orchestrator files already present in the project. Do not clone, download, or fetch another orchestrator. Do not modify `AGENTS.md`, and do not implement application features during setup.

After setup, verify all artifacts required by the bootstrap prompt and report created files, validation results, and any missing credentials.
