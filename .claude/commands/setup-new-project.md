---
description: Bootstrap BuilderX orchestration for a newly created project
---

Read `CLAUDE.md`, `AGENTS.md`, and `ROOT_WORKSPACE_GENERATION_POLICY.md` fully.

Then read `.codex/prompts/bootstrap-orchestrator.md` fully and execute it as the canonical bootstrap contract for this new project.

Use only orchestrator files already installed in the project. Do not clone, download, or fetch another orchestrator. Do not modify `AGENTS.md`. Create and verify the mandatory agent, registry, graph, memory, observability, and validation infrastructure before any application feature generation begins.

Do not implement application features during this setup command. After bootstrap, report created files, validation results, selected memory provider, and any missing credentials. BuilderX will provide the application instruction separately after setup succeeds.
