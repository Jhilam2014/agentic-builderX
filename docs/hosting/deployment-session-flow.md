# Deployment Session Flow

Hosting sessions are resumable records with a current stage, messages, selected project, provider, stack, region, credential status, permission preview, deployment plan, approval status, deployment status, deployment URL, and rollback availability.

Deployment cannot execute until a plan is generated and the user explicitly approves it.
